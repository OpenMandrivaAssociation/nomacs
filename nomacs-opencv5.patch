diff -ruN a/ImageLounge/plugins/AffineTransformations/src/DkSkewEstimator.cpp ImageLounge/plugins/AffineTransformations/src/DkSkewEstimator.cpp
--- a/ImageLounge/plugins/AffineTransformations/src/DkSkewEstimator.cpp	2025-12-28 14:26:24.000000000 +0100
+++ b/ImageLounge/plugins/AffineTransformations/src/DkSkewEstimator.cpp	2026-08-14 19:29:08.889158208 +0200
@@ -32,7 +32,6 @@
 #include <QRandomGenerator>
 
 #ifdef WITH_OPENCV
-#include "opencv2/imgproc/imgproc_c.h"
 #endif
 
 namespace nmp
@@ -91,7 +90,7 @@
         cv::Mat matGray;
 
         if (mMatImg.channels() > 1)
-            cv::cvtColor(mMatImg, matGray, CV_BGR2GRAY);
+            cv::cvtColor(mMatImg, matGray, cv::COLOR_BGR2GRAY);
         else
             matGray = mMatImg;
 
@@ -224,7 +223,7 @@
 
     // for displaying:
     // cv::normalize(separability, separability, 0, 255, NORM_MINMAX, CV_8UC1);
-    // cvtColor(separability, separability, CV_GRAY2RGB);
+    // cvtColor(separability, separability, cv::COLOR_GRAY2RGB);
 
     return separability;
 }
diff -ruN a/ImageLounge/plugins/CompositePlugin/src/SbChannelWidget.cpp ImageLounge/plugins/CompositePlugin/src/SbChannelWidget.cpp
--- a/ImageLounge/plugins/CompositePlugin/src/SbChannelWidget.cpp	2025-12-28 14:26:24.000000000 +0100
+++ b/ImageLounge/plugins/CompositePlugin/src/SbChannelWidget.cpp	2026-08-14 19:29:08.889391111 +0200
@@ -14,7 +14,6 @@
 #include <QSettings>
 
 #include "opencv2/imgproc/imgproc.hpp"
-#include "opencv2/imgproc/imgproc_c.h"
 
 namespace nmc
 {
@@ -77,7 +76,7 @@
         mSrcFormat = qImg.copy(0, 0, 1, 1);
 
         mImg = DkImage::qImage2Mat(qImg);
-        cv::cvtColor(mImg, mImg, CV_RGB2GRAY);
+        cv::cvtColor(mImg, mImg, cv::COLOR_RGB2GRAY);
 
         updateThumbnail();
         QFileInfo fi(file);
diff -ruN a/ImageLounge/plugins/CompositePlugin/src/SbCompositePlugin.cpp ImageLounge/plugins/CompositePlugin/src/SbCompositePlugin.cpp
--- a/ImageLounge/plugins/CompositePlugin/src/SbCompositePlugin.cpp	2025-12-28 14:26:24.000000000 +0100
+++ b/ImageLounge/plugins/CompositePlugin/src/SbCompositePlugin.cpp	2026-08-14 19:29:08.889604643 +0200
@@ -30,7 +30,6 @@
 #include <QSettings>
 
 #include "opencv2/imgproc/imgproc.hpp"
-#include "opencv2/imgproc/imgproc_c.h"
 
 /*******************************************************************************************************
  * SbCompositePlugin	- enter the plugin class name (e.g. DkPageExtractionPlugin)
@@ -231,9 +230,9 @@
         mAlpha = DkImage::qImage2Mat(_alpha);
         // currently it seems like qImage2Mat converts a single-channel QImage to a multi-channel Mat. so..
         if (mAlpha.channels() == 4)
-            cv::cvtColor(mAlpha, mAlpha, CV_RGBA2GRAY);
+            cv::cvtColor(mAlpha, mAlpha, cv::COLOR_RGBA2GRAY);
         else if (mAlpha.channels() == 3)
-            cv::cvtColor(mAlpha, mAlpha, CV_RGB2GRAY);
+            cv::cvtColor(mAlpha, mAlpha, cv::COLOR_RGB2GRAY);
     }
 }
 
diff -ruN a/ImageLounge/plugins/FakeMiniaturesPlugin/src/DkFakeMiniaturesDialog.h ImageLounge/plugins/FakeMiniaturesPlugin/src/DkFakeMiniaturesDialog.h
--- a/ImageLounge/plugins/FakeMiniaturesPlugin/src/DkFakeMiniaturesDialog.h	2025-12-28 14:26:24.000000000 +0100
+++ b/ImageLounge/plugins/FakeMiniaturesPlugin/src/DkFakeMiniaturesDialog.h	2026-08-14 19:32:31.808042242 +0200
@@ -34,6 +34,7 @@
 #ifdef WITH_OPENCV
 #include "opencv2/core/core.hpp"
 #include "opencv2/imgproc/imgproc.hpp"
+#include <opencv2/geometry.hpp>
 #endif
 
 class QSpinBox;
diff -ruN a/ImageLounge/plugins/PageExtractionPlugin/src/DkPageSegmentation.cpp ImageLounge/plugins/PageExtractionPlugin/src/DkPageSegmentation.cpp
--- a/ImageLounge/plugins/PageExtractionPlugin/src/DkPageSegmentation.cpp	2025-12-28 14:26:24.000000000 +0100
+++ b/ImageLounge/plugins/PageExtractionPlugin/src/DkPageSegmentation.cpp	2026-08-14 19:32:31.807201932 +0200
@@ -30,7 +30,7 @@
 #include <QPainterPath>
 
 #include "opencv2/imgproc/imgproc.hpp"
-#include "opencv2/imgproc/imgproc_c.h"
+#include <opencv2/geometry.hpp>
 
 namespace nmp
 {
@@ -90,7 +90,7 @@
         if (mScale == 1.0f && 960.0f / mImg.cols < 0.8f)
             mScale = 960.0f / mImg.cols;
 
-        cv::cvtColor(mImg, imgLab, CV_RGB2Lab); // boost colors
+        cv::cvtColor(mImg, imgLab, cv::COLOR_RGB2Lab); // boost colors
         lImg = findRectangles(mImg, mRects);
     }
 
@@ -102,7 +102,7 @@
     cv::Mat tImg, gray;
 
     if (mScale != 1.0f)
-        cv::resize(img, tImg, cv::Size(), mScale, mScale, CV_INTER_AREA); // inter nn -> assuming resize to be 1/(2^n)
+        cv::resize(img, tImg, cv::Size(), mScale, mScale, cv::INTER_AREA); // inter nn -> assuming resize to be 1/(2^n)
     else
         tImg = img;
 
@@ -138,7 +138,7 @@
             }
 
             // find contours and store them all as a list
-            findContours(gray, contours, CV_RETR_LIST, CV_CHAIN_APPROX_SIMPLE);
+            findContours(gray, contours, cv::RETR_LIST, cv::CHAIN_APPROX_SIMPLE);
 
             if (mLooseDetection) {
                 std::vector<std::vector<cv::Point>> hull;
@@ -160,7 +160,7 @@
 
             // DEBUG ------------------------
             // cv::Mat pImg = image.clone();
-            // cv::cvtColor(pImg, pImg, CV_Lab2RGB);
+            // cv::cvtColor(pImg, pImg, cv::COLOR_Lab2RGB);
             // DEBUG ------------------------
 
             // test each contour
@@ -196,7 +196,7 @@
                 }
             }
             // DEBUG ------------------------
-            // cv::cvtColor(pImg, pImg, CV_RGB2BGR);
+            // cv::cvtColor(pImg, pImg, cv::COLOR_RGB2BGR);
             // DkIP::imwrite("polyImg" + DkUtils::stringify(c) + "-" + DkUtils::stringify(l) + ".png", pImg);
             // DEBUG ------------------------
         }
diff -ruN a/ImageLounge/plugins/PageExtractionPlugin/src/DkPageSegmentationUtils.cpp ImageLounge/plugins/PageExtractionPlugin/src/DkPageSegmentationUtils.cpp
--- a/ImageLounge/plugins/PageExtractionPlugin/src/DkPageSegmentationUtils.cpp	2025-12-28 14:26:24.000000000 +0100
+++ b/ImageLounge/plugins/PageExtractionPlugin/src/DkPageSegmentationUtils.cpp	2026-08-14 19:32:31.807698388 +0200
@@ -25,7 +25,7 @@
 #include "DkPageSegmentationUtils.h"
 
 #include "opencv2/imgproc/imgproc.hpp"
-#include "opencv2/imgproc/imgproc_c.h"
+#include <opencv2/geometry.hpp>
 
 #include <algorithm>
 
@@ -433,9 +433,9 @@
 {
     cv::Mat gray, bw;
 
-    cv::cvtColor(img, gray, CV_RGB2GRAY);
+    cv::cvtColor(img, gray, cv::COLOR_RGB2GRAY);
     if (scale != 1.0f) {
-        cv::resize(gray, gray, cv::Size(), scale, scale, CV_INTER_AREA); // inter nn -> assuming resize to be 1/(2^n)
+        cv::resize(gray, gray, cv::Size(), scale, scale, cv::INTER_AREA); // inter nn -> assuming resize to be 1/(2^n)
     }
     const int smallerSide = std::min(gray.size().width, gray.size().height);
 
diff -ruN a/ImageLounge/src/DkCore/DkBasicLoader.cpp ImageLounge/src/DkCore/DkBasicLoader.cpp
--- a/ImageLounge/src/DkCore/DkBasicLoader.cpp	2025-12-28 14:26:24.000000000 +0100
+++ b/ImageLounge/src/DkCore/DkBasicLoader.cpp	2026-08-14 19:30:54.662580294 +0200
@@ -50,7 +50,6 @@
 #ifdef WITH_OPENCV
 #include "opencv2/core/core.hpp"
 #include "opencv2/imgproc/imgproc.hpp"
-#include "opencv2/imgproc/imgproc_c.h"
 
 #ifdef WITH_LIBRAW
 #include <libraw/libraw.h>
@@ -835,7 +834,7 @@
         case DRIF_FMT_BGR888: {
             cv::Mat imgMat = cv::Mat((int)h, (int)w, CV_8UC3, imgBytes);
             cv::Mat rgbMat = cv::Mat((int)h, (int)w, CV_8UC3, reinterpret_cast<uint8_t *>(img.bits()));
-            cv::cvtColor(imgMat, rgbMat, CV_BGR2RGB);
+            cv::cvtColor(imgMat, rgbMat, cv::COLOR_BGR2RGB);
 
             success = true;
         } break;
@@ -869,7 +868,7 @@
         case DRIF_FMT_BGRA8888: {
             cv::Mat imgMat = cv::Mat((int)h, (int)w, CV_8UC4, imgBytes);
             cv::Mat rgbMat = cv::Mat((int)h, (int)w, CV_8UC3, reinterpret_cast<uint8_t *>(img.bits()));
-            cv::cvtColor(imgMat, rgbMat, CV_BGR2RGB, 3);
+            cv::cvtColor(imgMat, rgbMat, cv::COLOR_BGR2RGB, 3);
 
             success = true;
         } break;
@@ -877,7 +876,7 @@
         case DRIF_FMT_RGBA8888: {
             cv::Mat imgMat = cv::Mat((int)h, (int)w, CV_8UC4, imgBytes);
             cv::Mat rgbMat = cv::Mat((int)h, (int)w, CV_8UC3, reinterpret_cast<uint8_t *>(img.bits()));
-            cv::cvtColor(imgMat, rgbMat, CV_RGBA2RGB, 3);
+            cv::cvtColor(imgMat, rgbMat, cv::COLOR_RGBA2RGB, 3);
 
             success = true;
         } break;
@@ -885,7 +884,7 @@
         case DRIF_FMT_GRAY: {
             cv::Mat imgMat = cv::Mat((int)h, (int)w, CV_8UC1, imgBytes);
             cv::Mat rgbMat = cv::Mat((int)h, (int)w, CV_8UC3, reinterpret_cast<uint8_t *>(img.bits()));
-            cv::cvtColor(imgMat, rgbMat, CV_GRAY2RGB);
+            cv::cvtColor(imgMat, rgbMat, cv::COLOR_GRAY2RGB);
 
             success = true;
         } break;
@@ -893,7 +892,7 @@
         case DRIF_FMT_YUV420P: {
             cv::Mat imgMat = cv::Mat((int)h + h / 2, (int)w, CV_8UC1, imgBytes);
             cv::Mat rgbMat = cv::Mat((int)h, (int)w, CV_8UC3, reinterpret_cast<uint8_t *>(img.bits()));
-            cv::cvtColor(imgMat, rgbMat, CV_YUV2RGB_I420);
+            cv::cvtColor(imgMat, rgbMat, cv::COLOR_YUV2RGB_I420);
 
             success = true;
         } break;
@@ -901,7 +900,7 @@
         case DRIF_FMT_YVU420P: {
             cv::Mat imgMat = cv::Mat((int)h + h / 2, (int)w, CV_8UC1, imgBytes);
             cv::Mat rgbMat = cv::Mat((int)h, (int)w, CV_8UC3, reinterpret_cast<uint8_t *>(img.bits()));
-            cv::cvtColor(imgMat, rgbMat, CV_YUV2RGB_YV12);
+            cv::cvtColor(imgMat, rgbMat, cv::COLOR_YUV2RGB_YV12);
 
             success = true;
         } break;
@@ -909,7 +908,7 @@
         case DRIF_FMT_NV12: {
             cv::Mat imgMat = cv::Mat((int)h + h / 2, (int)w, CV_8UC1, imgBytes);
             cv::Mat rgbMat = cv::Mat((int)h, (int)w, CV_8UC3, reinterpret_cast<uint8_t *>(img.bits()));
-            cv::cvtColor(imgMat, rgbMat, CV_YUV2RGB_NV12);
+            cv::cvtColor(imgMat, rgbMat, cv::COLOR_YUV2RGB_NV12);
 
             success = true;
         } break;
@@ -917,7 +916,7 @@
         case DRIF_FMT_NV21: {
             cv::Mat imgMat = cv::Mat((int)h + h / 2, (int)w, CV_8UC1, imgBytes);
             cv::Mat rgbMat = cv::Mat((int)h, (int)w, CV_8UC3, reinterpret_cast<uint8_t *>(img.bits()));
-            cv::cvtColor(imgMat, rgbMat, CV_YUV2RGB_NV21);
+            cv::cvtColor(imgMat, rgbMat, cv::COLOR_YUV2RGB_NV21);
 
             success = true;
         } break;
@@ -2315,14 +2314,14 @@
 
         // define bayer pattern
         if (type == 180) {
-            cvtColor(rawMat, rgbImg, CV_BayerBG2RGB); // bitmask  10 11 01 00  -> 3(G) 2(B) 1(G) 0(R) ->	RG RG RG
+            cvtColor(rawMat, rgbImg, cv::COLOR_BayerBG2RGB); // bitmask  10 11 01 00  -> 3(G) 2(B) 1(G) 0(R) ->	RG RG RG
                                                       //													GB GB GB
         } else if (type == 30) {
-            cvtColor(rawMat, rgbImg, CV_BayerRG2RGB); // bitmask  00 01 11 10	-> 0 1 3 2
+            cvtColor(rawMat, rgbImg, cv::COLOR_BayerRG2RGB); // bitmask  00 01 11 10	-> 0 1 3 2
         } else if (type == 225) {
-            cvtColor(rawMat, rgbImg, CV_BayerGB2RGB); // bitmask  11 10 00 01
+            cvtColor(rawMat, rgbImg, cv::COLOR_BayerGB2RGB); // bitmask  11 10 00 01
         } else if (type == 75) {
-            cvtColor(rawMat, rgbImg, CV_BayerGR2RGB); // bitmask  01 00 10 11
+            cvtColor(rawMat, rgbImg, cv::COLOR_BayerGR2RGB); // bitmask  01 00 10 11
         } else {
             qWarning() << "Wrong Bayer Pattern (not BG, RG, GB, GR)\n";
             return cv::Mat();
@@ -2498,7 +2497,7 @@
         // revert back to 8-bit image
         img.convertTo(img, CV_8U);
 
-        cv::cvtColor(img, img, CV_RGB2YCrCb);
+        cv::cvtColor(img, img, cv::COLOR_RGB2YCrCb);
 
         std::vector<cv::Mat> imgCh;
         cv::split(img, imgCh);
@@ -2508,7 +2507,7 @@
         cv::medianBlur(imgCh[2], imgCh[2], winSize);
 
         cv::merge(imgCh, img);
-        cv::cvtColor(img, img, CV_YCrCb2RGB);
+        cv::cvtColor(img, img, cv::COLOR_YCrCb2RGB);
         qDebug() << "median blur takes:" << dt;
     }
 }
@@ -2524,7 +2523,7 @@
 
     // TODO: for now - fix this!
     if (img.channels() == 1)
-        cv::cvtColor(img, img, CV_GRAY2RGB);
+        cv::cvtColor(img, img, cv::COLOR_GRAY2RGB);
 
     QImage src;
     src.setColorSpace(QColorSpace{QColorSpace::SRgb}); // for output_color=1
diff -ruN a/ImageLounge/src/DkCore/DkImageStorage.cpp ImageLounge/src/DkCore/DkImageStorage.cpp
--- a/ImageLounge/src/DkCore/DkImageStorage.cpp	2025-12-28 14:26:24.000000000 +0100
+++ b/ImageLounge/src/DkCore/DkImageStorage.cpp	2026-08-14 19:30:54.673206821 +0200
@@ -44,7 +44,6 @@
 
 #ifdef WITH_OPENCV
 #include "opencv2/imgproc/imgproc.hpp"
-#include "opencv2/imgproc/imgproc_c.h"
 #endif
 
 #include <cmath>
@@ -135,22 +134,22 @@
     }
 #ifdef WITH_OPENCV
 
-    int ipl = CV_INTER_CUBIC;
+    int ipl = cv::INTER_CUBIC;
     switch (interpolation) {
     case ipl_nearest:
-        ipl = CV_INTER_NN;
+        ipl = cv::INTER_NEAREST;
         break;
     case ipl_area:
-        ipl = CV_INTER_AREA;
+        ipl = cv::INTER_AREA;
         break;
     case ipl_linear:
-        ipl = CV_INTER_LINEAR;
+        ipl = cv::INTER_LINEAR;
         break;
     case ipl_cubic:
-        ipl = CV_INTER_CUBIC;
+        ipl = cv::INTER_CUBIC;
         break;
     case ipl_lanczos:
-        ipl = CV_INTER_LANCZOS4;
+        ipl = cv::INTER_LANCZOS4;
         break;
     }
 
@@ -555,7 +554,7 @@
 #ifdef WITH_OPENCV
 
     cv::Mat cvImg = DkImage::qImage2Mat(img);
-    cv::cvtColor(cvImg, cvImg, CV_RGB2Lab);
+    cv::cvtColor(cvImg, cvImg, cv::COLOR_RGB2Lab);
 
     std::vector<cv::Mat> imgs;
     cv::split(cvImg, imgs);
@@ -565,7 +564,7 @@
         cvImg = imgs[0];
 
     // convert it back for the painter
-    cv::cvtColor(cvImg, cvImg, CV_GRAY2RGB);
+    cv::cvtColor(cvImg, cvImg, cv::COLOR_GRAY2RGB);
 
     imgR = DkImage::mat2QImage(cvImg, img);
 #else
@@ -954,9 +953,9 @@
     cv::Mat hsvImg = DkImage::qImage2Mat(src);
 
     if (hsvImg.channels() > 3)
-        cv::cvtColor(hsvImg, hsvImg, CV_RGBA2BGR);
+        cv::cvtColor(hsvImg, hsvImg, cv::COLOR_RGBA2BGR);
 
-    cv::cvtColor(hsvImg, hsvImg, CV_BGR2HSV);
+    cv::cvtColor(hsvImg, hsvImg, cv::COLOR_BGR2HSV);
 
     // apply hue/saturation changes
     for (int rIdx = 0; rIdx < hsvImg.rows; rIdx++) {
@@ -990,7 +989,7 @@
         }
     }
 
-    cv::cvtColor(hsvImg, hsvImg, CV_HSV2BGR);
+    cv::cvtColor(hsvImg, hsvImg, cv::COLOR_HSV2BGR);
     imgR = DkImage::mat2QImage(hsvImg, src);
 
 #endif // WITH_OPENCV
@@ -1010,7 +1009,7 @@
     rgbImg.convertTo(rgbImg, CV_16U, 256, offset * std::numeric_limits<unsigned short>::max());
 
     if (rgbImg.channels() > 3)
-        cv::cvtColor(rgbImg, rgbImg, CV_RGBA2BGR);
+        cv::cvtColor(rgbImg, rgbImg, cv::COLOR_RGBA2BGR);
 
     if (exposure != 0.0)
         rgbImg = exposureMat(rgbImg, exposure);
@@ -1540,13 +1539,13 @@
                       (int)img.step,
                       QImage::Format_Indexed8); // opencv uses size_t for scaling in x64 applications
         // Mat tmp;
-        // cvtColor(img, tmp, CV_GRAY2RGB);	// Qt does not support writing to index8 images
+        // cvtColor(img, tmp, cv::COLOR_GRAY2RGB);	// Qt does not support writing to index8 images
         // img = tmp;
         if (!srcImg.colorTable().isEmpty())
             qImg.setColorTable(srcImg.colorTable());
     }
     if (img.type() == CV_8UC3) {
-        // cv::cvtColor(img, img, CV_RGB2BGR);
+        // cv::cvtColor(img, img, cv::COLOR_RGB2BGR);
         qImg = QImage(img.data, (int)img.cols, (int)img.rows, (int)img.step, QImage::Format_RGB888);
     }
     if (img.type() == CV_8UC4) {
@@ -1663,7 +1662,7 @@
         }
     }
 
-    cv::remap(src, dst, mapx, mapy, CV_INTER_AREA, IPL_BORDER_REPLICATE);
+    cv::remap(src, dst, mapx, mapy, cv::INTER_AREA, cv::BORDER_REPLICATE);
 }
 
 void DkImage::tinyPlanet(QImage &img, double scaleLog, double angle, QSize s, bool invert /* = false */)
@@ -1886,7 +1885,7 @@
     try {
         cv::Mat rImgCv = DkImage::qImage2Mat(resizedImg);
         cv::Mat tmp;
-        cv::resize(rImgCv, tmp, cv::Size(s.width(), s.height()), 0, 0, CV_INTER_AREA);
+        cv::resize(rImgCv, tmp, cv::Size(s.width(), s.height()), 0, 0, cv::INTER_AREA);
         resizedImg = DkImage::mat2QImage(tmp, resizedImg);
     } catch (...) {
         qWarning() << "imageStorageScaleToSize: OpenCV exception caught while resizing...";
diff -ruN a/ImageLounge/src/DkGui/DkDialog.cpp ImageLounge/src/DkGui/DkDialog.cpp
--- a/ImageLounge/src/DkGui/DkDialog.cpp	2025-12-28 14:26:24.000000000 +0100
+++ b/ImageLounge/src/DkGui/DkDialog.cpp	2026-08-14 19:29:08.891272013 +0200
@@ -92,7 +92,6 @@
 
 #ifdef WITH_OPENCV
 #include "opencv2/imgproc/imgproc.hpp"
-#include "opencv2/imgproc/imgproc_c.h"
 #endif
 
 #ifdef WITH_QUAZIP
@@ -3209,7 +3208,7 @@
 
     mImg = mImg.rowRange(qFloor(shR), mImg.rows - qCeil(shR)).colRange(qFloor(shC), mImg.cols - qCeil(shC));
     cv::Mat mImgLab;
-    cv::cvtColor(mImg, mImgLab, CV_RGB2Lab);
+    cv::cvtColor(mImg, mImgLab, cv::COLOR_RGB2Lab);
     std::vector<cv::Mat> channels;
     cv::split(mImgLab, channels);
     cv::Mat imgL = channels[0];
@@ -3317,7 +3316,7 @@
                     // debug
                     cv::Mat imgT3;
                     cv::merge(channels, imgT3);
-                    cv::cvtColor(imgT3, imgT3, CV_Lab2BGR);
+                    cv::cvtColor(imgT3, imgT3, cv::COLOR_Lab2BGR);
                     emit updateImage(DkImage::mat2QImage(imgT3, mSrcImg));
                 }
 
@@ -3435,7 +3434,7 @@
         img = thumb;
 
     cv::Mat cvThumb = DkImage::qImage2Mat(img);
-    cv::cvtColor(cvThumb, cvThumb, CV_RGB2Lab);
+    cv::cvtColor(cvThumb, cvThumb, cv::COLOR_RGB2Lab);
     std::vector<cv::Mat> channels;
     cv::split(cvThumb, channels);
     cvThumb = channels[0];
@@ -3455,7 +3454,7 @@
     if (cvThumb.rows < patchRes || cvThumb.cols < patchRes)
         qDebug() << "enlarging thumbs!!";
 
-    cv::resize(cvThumb, cvThumb, cv::Size(patchRes, patchRes), 0.0, 0.0, CV_INTER_AREA);
+    cv::resize(cvThumb, cvThumb, cv::Size(patchRes, patchRes), 0.0, 0.0, cv::INTER_AREA);
 
     return cvThumb;
 }
@@ -3562,7 +3561,7 @@
             origR = mOrigImg.clone();
             mosaicR = mMosaicMatSmall.clone();
         } else {
-            cv::resize(mOrigImg, origR, mMosaicMat.size(), 0, 0, CV_INTER_LANCZOS4);
+            cv::resize(mOrigImg, origR, mMosaicMat.size(), 0, 0, cv::INTER_LANCZOS4);
             mosaicR = mMosaicMat;
             mOrigImg.release();
         }
@@ -3601,7 +3600,7 @@
 
         // if (!computePreview)
         //	mosaicMat.release();
-        cv::cvtColor(origR, origR, CV_Lab2BGR);
+        cv::cvtColor(origR, origR, cv::COLOR_Lab2BGR);
         qDebug() << "color converted";
 
         mMosaic = DkImage::mat2QImage(origR, mSrcImg);
diff -ruN a/ImageLounge/src/DkGui/DkViewPort.cpp ImageLounge/src/DkGui/DkViewPort.cpp
--- a/ImageLounge/src/DkGui/DkViewPort.cpp	2025-12-28 14:26:24.000000000 +0100
+++ b/ImageLounge/src/DkGui/DkViewPort.cpp	2026-08-14 19:29:08.893959946 +0200
@@ -60,7 +60,6 @@
 
 #ifdef WITH_OPENCV
 #include "opencv2/imgproc/imgproc.hpp"
-#include "opencv2/imgproc/imgproc_c.h"
 #endif
 
 #ifdef Q_OS_WIN
@@ -2539,7 +2538,7 @@
         }
         // The first element in the vector contains the gray scale 'average' of the 3 channels:
         cv::Mat grayMat;
-        cv::cvtColor(imgUC3, grayMat, CV_BGR2GRAY);
+        cv::cvtColor(imgUC3, grayMat, cv::COLOR_BGR2GRAY);
         mImgs[0] = QImage((const unsigned char *)grayMat.data,
                           (int)grayMat.cols,
                           (int)grayMat.rows,
